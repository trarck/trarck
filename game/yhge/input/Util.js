(function(){    yhge.input.getOffset=function(ele){        var left=ele.offsetLeft,top=ele.offsetTop;        var p=ele.offsetParent;        while(p){            left+=p.offsetLeft;            top+=p.offsetTop;            p=p.offsetParent;        }        return {left:left,top:top};    };    yhge.input.InputType={        Mouse:1,        Touch:2    };    var inputType=inputType();    yhge.input.inputType=inputType;    yhge.input.getInputType=function(){        return inputType;    };    function inputType(){        var flag = yhge.input.InputType.Mouse;        var agent = navigator.userAgent.toLowerCase();        var keywords = ["android", "iphone", "ipod", "ipad", "windows phone"];        for (var i in keywords) {            var item=keywords[i];            if (agent.indexOf(item) > -1 ) {                flag = yhge.input.InputType.Touch;                break;            }        }        return flag;    }})();