(function  () {    var Scheduler=yhge.times.Scheduler;    //根据不同平台实现定时器功能。    var Director=function  () {        this.initialize.apply(this,arguments);    };    Director.prototype={        classname:"Director",        initialize:function  (scheduler) {            scheduler.addTask(this._run,Scheduler.Render,this);        },        _run:function  (delta) {            //update scene draw            //scene animate next scene            this._scene.render();        },        setScene:function  (scene) {            this._scene=scene;            return this;        }    };    Director.getInstance=function  (scheduler) {        if (!this._instance) {            this._instance = new Director(scheduler);        }        return this._instance;    };    yhge.scene.Director=Director})();