对于像canvas中画的图形添加鼠标事件，不必遍历每个图形。把整个画布分成几大块，如果鼠标落在哪块，只要遍历该块里的图形。
如果鼠标落在边缘(精确：边距为最大图形的大小，粗略：平均值或固定值)，则也对相邻区块的图形遍历。比全部遍历效率要高。
同样适应其它情况。
比如像google地图中，如果地图上显示的mark太多，会用服务器成成静态图。
鼠标经过某个mark的时候要高亮，并提示信息。这样也不用遍历所有mark，查看鼠标所在大块内的mark。
这样计算量要小很多。由于这种mark很小，边缘可以忽略(边距很小)。
